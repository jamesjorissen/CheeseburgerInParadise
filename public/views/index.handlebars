<h1>Let's eat a freaking burger!</h1>

<h3 class="availableoptions">These are the options: </h2>

<container class="availableburgers">
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{>burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</container>

<container class="eaten">
<h2 class="eatenburgers">These burgers were DEVOURED</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    <ul>
      {{name_burger}}
</ul>
    {{/if}}
  {{/each}}
</ul>
</container>

<br>
<br>
<h2>Create your own burger masterpiece!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="br">Burger Name: </label>
    <input type="text" id="br" name="name">
  </div>
  <button type="submit">Submit</button>
    <h3>Enter a Burger you want to eat, click the "Submit" button</h3>
</form>

  